<div class="p-4 sm:px-6 lg:px-8 max-w-7xl m-auto">
  <h1 class="text-base font-semibold leading-7 text-gray-900"><%= @user.username %>'s profile </h1>

    <div class="flex justify-center">
      <%= gravatar_for @user, size: 200 %>
    </div>
</div>
